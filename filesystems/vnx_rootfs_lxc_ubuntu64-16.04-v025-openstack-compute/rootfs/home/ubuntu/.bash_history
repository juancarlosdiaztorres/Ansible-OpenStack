halt -p
sudo halt -p
sudo adduser vnx
sudo adduser vnx sudo
sudo passwd root
sudo apt-get update
sudo apt-get dist-upgrade
sudo apt-get install aptsh openssh-server traceroute telnet nmap apache2 wget tcpdump
update-rc.d -f apache2 remove   # to avoid automatic start
sudo update-rc.d -f apache2 remove   # to avoid automatic start
vi /etc/hosts
sudo vi /etc/hosts
sudo vi /etc/hostname 
vi /etc/network/interfaces
sudo vi /etc/network/interfaces
halt -p
sudo halt -p
