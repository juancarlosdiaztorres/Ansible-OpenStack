#!/bin/sh

set -e

if [ "$1" = "configure" ] ; then
	update-alternatives --install /usr/bin/gunicorn_pecan gunicorn_pecan /usr/bin/gunicorn_pecan-python2 300
	update-alternatives --install /usr/bin/pecan pecan /usr/bin/pecan-python2 300
fi


# Automatically added by dh_python2:
if which pycompile >/dev/null 2>&1; then
	pycompile -p python-pecan 
fi

# End automatically added section


exit 0
